# <%= it.name %>

<%= it.doc %>

## Version
<%= it.version %>

## Depends
<% it.depends?.forEach((type) => { %>
- [<%= type.name %>](/<%= type.link %>)

<% }) %>

## Types

<% it.types.forEach((type) => { %>
- [<%= type.typename %>](/<%= it.link %>)

<% }) %>
