# <%= it.type.typename %>

<%= it.type.doc %>

<% if (it.type.markers.length) { %>
## Markers
<table>
<tbody>
<% it.type.markers.forEach((name) => { %>
<tr>
<td className='p-2 align-top'><span className='bg-sky-200 rounded-md px-3 py-1 text-sky-700 block font-semibold text-center text-sm'><%= name %> </span></td>
<td className='p-2 align-top'><%= it.hsDocs.get(name)?.replaceAll(/\n/g, ' ') ?? '' %></td>
</tr>
<% }) %>
</tbody>
</table>
<% } %>

<% if (Object.keys(it.type.slots).length) { %>
## Slots

<% Object.entries(it.type.slots).forEach(([name, type]) => { %>
### <%= name %>

Type: `<%= type.typename %>`

<% }) %>
<% } %>

<% if (it.type.points.length) { %>
## Points

<% it.type.points.forEach((type) => { %>
### <%= type.typename %>

Type: `<%= type.typename %>`

<% }) %>
<% } %>

<% if (it.type.allSuperTypes.length) { %>
## Super Types
<% it.type.allSuperTypes.forEach((type) => { %>
### <%= type.typename %>

<%= type.doc %>
[More...](/<%= type.link %>)

<% }) %>
<% } %>

<% if (it.type.allSubtypes.length) { %>
## Subtypes
<% it.type.allSubtypes.forEach((type) => { %>
### <%= type.typename %>

<%= type.doc %>
[More...](./<%= type.typename %>)
<% }) %>
<% } %>
